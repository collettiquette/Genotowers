genotower.path = {
    
    convertGrid : function (oldGrid) {
        var x = 0,
            y = 0;

        for (x = 0; x < genotower.config.MAP_WIDTH; x += 1) {
            
            for (y = 0; y < genotower.config.MAP_HEIGHT; y += 1) {
                
            }
        }
    },
    
    checkPath: function () {
        var pathFinder = new EasyStar.js();
    }
};